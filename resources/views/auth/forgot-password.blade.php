<x-guest-layout><div class="hn-auth-heading"><span>استعادة الحساب</span><h2>نسيت كلمة المرور؟</h2><p>أدخل بريدك وسنرسل إليك رابطًا آمنًا لاختيار كلمة مرور جديدة.</p></div>@if(session('status'))<div class="alert alert-success">{{ session('status') }}</div>@endif<form method="POST" action="{{ route('password.email') }}">@csrf<div class="form-group"><label for="email">البريد الإلكتروني</label><input id="email" class="form-control @error('email') is-invalid @enderror" type="email" name="email" value="{{ old('email') }}" required autofocus placeholder="name@example.com">@error('email')<div class="invalid-feedback">{{ $message }}</div>@enderror</div><button class="hn-btn hn-btn-primary hn-auth-submit" type="submit">إرسال رابط الاستعادة</button><p class="hn-auth-switch"><a href="{{ route('login') }}">العودة لتسجيل الدخول</a></p></form></x-guest-layout>
